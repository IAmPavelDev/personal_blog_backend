import { ReturnImageT } from './types/ReturnImageT';
import { Controller, Get, Param, Req } from '@nestjs/common';

@Controller('images')
export class ImageController {
    // constructor(){}
    // @Get(':imageId')
    // async getImageById(
    //     @Req() req: Request,
    //     @Param() { imageId }: { imageId: string },
    // ): Promise<ReturnImageT> {
    //     return {};
    // }
}
